# Stage3：暂停、解除显存限制、实测参数续跑

本次授权：先暂停当前训练，再解除显存比例限制测试，最后用通过测试的新参数继续八臂实验。

产物根目录：
`result/hkbz_train_logs/stage3_representation_all_20260908_r1/capacity_retest/uncapped_20260908_r1`。

## 状态接续与数据保留

先暂停原控制器、八个训练主进程和两个验证主进程；环境子进程完成待处理 IPC 后闲置。
为了释放暂停进程仍持有的显存，随后仅让原训练进程完成当前 8 轨迹更新组，在组边界导出其真实状态。
逐参数核对 Adam 步数与已提交更新账本，并检查策略更新次数、冻结参数、normalizer、状态来源和哈希。
模型、优化器、normalizer 和 RNG 使用真实进程导出的张量，不使用数值重建结果。

| 臂 | 已认证的真实恢复轨迹数 |
| --- | ---: |
| E0_T0 | 512 |
| E0_T1 | 512 |
| E1_T0 | 512 |
| E1_T1 | 272 |
| E2_T0 | 264 |
| E2_T1 | 264 |
| E3_T0 | 512 |
| E3_T1 | 512 |

本次迁移没有丢弃已提交更新组。每臂的 `boundaries/ARM/result.json`、`boundary_proof.json` 和
checkpoint 保留来源链。旧日志、模型、诊断导出和所有失败测试均保留。
这不抹去上一操作阶段由 GPU 独占检查造成的 856 轨迹回退；此前事件仍详见
`STAGE3_RESOURCE_PROBE_20260908.md`。

## 容量测试边界

- 训练 allocator 比例显式设为 1.0；小模型构造后、恢复和采样前解除原比例限制。冻结的模型/环境源码不改写。
- B32 为 32 条同一行为策略轨迹，分四波、每波 8 个环境收集；更新使用两轮 PPO。
- 基准测试源为 E2_T1@240。后续参数对照复用首次测试保存的完整轨迹，验证 checkpoint、策略版本、源码、案例/seed 和档案 SHA。
- 复用仅用于一次性容量诊断；不作为正式训练的数据重放，也不使用 probe 的更新后模型来恢复任一实验臂。
- 大图压力测试把四个已采样案例中输入图 tensor bytes 最大的案例扩展到 B32；有重复轨迹，仅作容量检查，不是独立 RL 样本，也不是全数据集最坏情况。
- 每个测试服务主存上限 24 GiB，23 GiB 前置保护；主机 MemAvailable 保留 24 GiB；最长 60 分钟。所有环境子进程计入限制。
- GPU/主存按约 5 秒采样；本机内核无 `memory.peak`，短暂峰值可能漏采，Torch 分配/保留峰值另有精确计数。
- 准入要求普通和大图测试均完成完整更新、KL 通过、进程 GPU 峰值不高于 20.5 GiB、测试 cgroup 主存峰值不高于 23 GiB。
  20.5 GiB 是选择参数时为共卡 validator 留余量的准入标准，不是正式训练的显存比例限制。
- 档案反序列化/深拷贝有额外主存成本。不能直接把档案测试的主存峰值乘八当作正式在线采样峰值。
- 重用档案的测试只报告 update 耗时，不能用档案加载时间代替 rollout 时间、宣称完整训练吞吐提升。

## 明确保留的失败及修正

1. B32/T16/4 GiB 缓存在比例 1.0、但旧暂停训练还占用约 1.69 GiB 时 OOM。
2. 释放全部原训练显存后，在独占卡上重新测试同一配置，仍 OOM：进程占用 23.49 GiB，卡仅余约 15.56 MiB，申请 54 MiB 失败。
   因此 T16 不进入续跑；这次结果已经排除原 75% 比例限制和原训练占用的影响。
3. 尝试按原动作轨迹数值重建已提交状态时，动作/成本虽一致，但某项 log-prob 指标出现约 1e-5 偏差；严格一致性检查拒绝该结果。
   未放宽公差，未使用重建 checkpoint。改用上表中原进程的真实状态导出与组边界认证。
4. 真状态捕获的第一次启动因缺少工作区根路径环境变量，在校验阶段退出，没有恢复训练；修正环境声明后第二次捕获成功。
5. 新增 CPU 流程测试最初因测试 mock 清理了首次加载的 torch 模块而失败；改为在 mock 前导入 torch，24 项测试通过。

## 新参数续跑的约束

新入口：`onpolicy/scripts/train/continue_stage3_capacity.py`；协议：`stage3_capacity_protocol.py`。
启动器会复制这些文件到本次产物目录，冻结 SHA，验证全部边界和容量证据后再启动。
原控制器/validator 仅在边界验证通过后退役；新进程使用独立服务和产物目录，不覆盖旧训练。

- 八臂与单 seed 不变，共享一个队列、两个异步 validator，位于 GPU0/GPU4；CPU 按 NUMA 绑定。
- 训练 batch 最大 32；T0 每次更新一个案例，T1 每次更新四个案例。相同起点的四组宏块保持案例/seed 暴露匹配。
- 使用新 seed 区段进行新鲜在线采样。为准确落在每 480 轨迹的验证边界，边界宏块使用小于 32 的平衡 batch。
- batch 变化会改变每个案例的重复数、相同轨迹预算内的更新次数与案例覆盖；各臂切换点也不同。
  因此这是记录了切换位置的自适应续跑，不能当成从头采用固定 B32 配置的干净因子实验。
- PPO epochs、学习率、clip、source advantage、探索方式、网络结构、C0、greedy 验证及准入门槛保持原设定。
- 每一个完整更新组保存模型、Adam、normalizer、RNG 和精确采样游标；只在保存完成后发布验证请求。
- 960 轨迹筛选：gain ≥0.5% 且风险通过，最多两臂延长；1440/1920 维持 ≥1% 和原风险阈值。不自动进入 4800，不开启 finalblind，不加多 seed。
- 八臂服务主存 MemoryHigh=96 GiB、MemoryMax=108 GiB，禁 swap；定期记录全服务主存，不把解除显存比例限制误当成解除 128 GiB 主机保护。
- 外部 GPU 作业冲突改为暂停本服务主进程并等待，不再因独占检查直接杀掉八臂。

容量通过只说明这些已测更新可执行，不能证明 RL 成本已经改善。后续以原 C0 配对验证结果判断科研目标。

Material Passport：academic-research-suite / experiment-agent；mode=run；origin_date=2026-09-08；
version=capacity_continuation_v1；真实边界已核验，最终参数及运行验收见本次产物目录的 `REPORT.md`。
