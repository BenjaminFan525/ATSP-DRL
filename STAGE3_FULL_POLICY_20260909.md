# Stage3 完整策略分离：单 seed、三臂、八卡研究

## 目标与对照

研究问题：上一轮 E2 仍共享冻结的底层表示。本轮保持 PPO 更新方法不变，检验“解冻全层”以及“完全分离各角色策略编码器”能否使 RL 明显改善原始 C0。

| 臂 | 图编码器 | 策略 GRU / actor | GPU |
| --- | --- | --- | --- |
| A_E2 | 共享冻结前缀、各角色独立可训练尾部 | 原有角色独立模块 | 0、1 |
| B_SHARED | 全层共享、全层可训练 | 原有角色独立模块 | 2、3、4 |
| C_PRIVATE | 三套全层独立、全层可训练 | 原有角色独立模块 | 5、6、7 |

C_PRIVATE 无跨角色共享策略参数；仍共享环境、联合自回归调度过程、可行性掩码和资源预约，不能把联合可行性拆成相互冲突的独立动作。Critic 仍为已有角色价值模块；source-relative actor 优势不依赖 critic baseline。B/C 从同一原始 C0 深复制，不随机初始化底层，不使用上一轮最优权重或诊断更新后的权重。

固定单 seed 2026090903；不增加多 seed、其他优化器臂、IGA 重跑或 Finalblind 暴露。数据、硬约束环境契约、C0 逐案例参考成本及 Tune60 沿用上一轮。

## 多卡数学契约

每臂只有一个逻辑策略。固定 global batch 从 24、12 按预注册容量规则选择：每批四案例、每案例等量新鲜随机轨迹；三臂的案例/seed/episode/更新次数完全相同。不同 GPU 只分片轨迹，不是多个独立实验，也不做 local SGD。

各卡完成完整循环历史重放和本地反向传播，再按全局“案例均值 → 轨迹均值 → 时间/角色求和”权重汇总梯度。先汇总、后全局范数裁剪、后 Adam；critic 同样使用全局决策数归一化。CPU/Gloo 只传梯度和小型统计，不跨卡搬运轨迹图。每次提交检查所有 rank 的模型、Adam、ValueNorm、更新计数完全相同。

固定 J 探索、source-relative 优势、actor LR 5e-6、critic LR 1e-4、PPO epochs 2、TBPTT 8、全局裁剪 1、PPO clip 0.2、KL 软阈值 0.02/硬阈值 0.04。若触发提前停止或跳过 actor 更新，则保存产物并停止准入/训练，不能悄悄产生不等更新预算。

容量选择在正式训练之前完成，之后不更改 global batch、world size 或更新配方。batch=24 时 960 episodes 对应 80 次 actor 更新；batch=12 时为 160 次，三臂一致。本轮可解释的是同轮架构对照，不能把与历史变 batch 实验的差值全部归因于架构。

## 启动与准入

1. 从历史已验证源码快照继承环境与通用策略实现，只叠加本轮 Stage3 文件；源码冻结副本严格校验，工作区并行基线实验修改仅提示、不阻断。
2. 三臂单卡参考：四个训练案例的 C0 greedy 成本不变；八条轨迹完整历史重放误差 ≤0.002；真实两轮 PPO；模型/Adam/ValueNorm/RNG 恢复重放一致；B/C 各完整底层实际更新。
3. 使用正式两卡/三卡划分重复同一组八条轨迹；轨迹动作及成本一致；单/多卡更新模型误差 ≤2e-6，Adam/ValueNorm atol=2e-6、rtol=2e-5；组内各 rank 完全一致。三臂 C0 Tune60 逐案例复现。
4. 所有训练 rank 同时进行 batch24 常规和密图容量测试，共享验证池保持驻留。密图是四个 canary 案例里观测图最大的案例，不声称覆盖整个数据集最坏情况。各 rank PyTorch reserved ≤19 GiB、全服务采样 RAM ≤96 GiB 方可准入；若仅容量余量不合格，测试预注册 batch12。实现、数学、KL 或 OOM 故障停止流程，保留记录，不自动重试掩盖问题。
5. 写入不可覆盖的 training_admission.json，记录固定 batch 和所有检查，正式 PPO **重新从原始 C0 开始**。本轮不重复历史 teacher-fit，也不加入隐性教师目标。

技术准入通过不等于研究目标实现。若准入失败，所有产物保留，正式训练不启动。

## 资源与验证

使用全部八张 24 GiB GPU，64 物理核/128 逻辑 CPU：每个训练 rank 七物理核；两位全局共享 validator 各三物理核，与 GPU0、GPU4 共卡；控制器两物理核。SMT 兄弟核不拆分。内存预算针对整套进程及其环境子进程，systemd MemoryHigh=96G、MemoryMax=108G、SwapMax=0，为 128G 主机保留系统余量；训练不施加旧的分数显存上限。

所有臂共享一个持久化队列、两个独立领取任务的异步消费者。验证权重由不可变 checkpoint 指向，缓存键含权重/源码/环境/案例/探索/评测协议身份。按每 480 episodes 提交，队列积压时反压训练，不在每个 GPU 建独立 validator。启动前等外部 GPU 作业释放，不抢占；运行时记录外部新增作业提示。每十秒记录全服务内存和资源状态。

## 检查点、恢复和停止

每个完整 global group 保存各 rank 的模型、双 Adam、角色 ValueNorm、Python/NumPy/Torch/CUDA RNG、采样游标和源码/配方身份；所有 rank 成功后才发布全局 commit。后续阶段只从这种完整 commit 恢复，不接受单卡半组/失败状态或 diagnostic-only 权重。轨迹动作、更新统计、命令、日志和失败状态均保留；服务不自动重启、不覆写旧实验。

## 科学门槛

先运行三臂至 960 episodes，Tune60 相对 C0 平均成本降低至少 0.5%，且 OOD-stress / stress_joint 回归 ≤0.5%、尾部回归 ≤1%、退化超过 5% 的案例比例 ≤5%，最多两臂进入后续。1440 与 1920 两个端点均须 gain≥1% 且风险门通过；预期显著实用改进为 ≥2%，不是把通过 0.5% 初筛等同达到目标。仍只形成单 seed 证据。

不合格臂停止并保留全部产物，不为了占满 GPU 增加无授权实验。无自动 4800 扩展或 Finalblind 验证。

启动命令：`bash onpolicy/scripts/train/launch_stage3_full_policy_all.sh`。默认输出 `result/hkbz_train_logs/stage3_full_policy_all_20260909_r1`；状态见 `status.json`，实际准入见 `training_admission.json`，科学初筛见 `screen_admission.json`。

## 2026-09-09 数值恢复修订

首次执行在多卡恢复准入停止，正式训练为 0 episodes。经固定输入反向传播对照定位到 CUDA 数值非确定性及图最大池化的扩展算子路径；修复和证据见 `STAGE3_NUMERICAL_RECOVERY_20260909.md`。三臂统一启用确定性后端与稳定的首次最大值梯度归属，误差阈值不放宽。恢复使用新目录记录同一方案，不改旧快照/日志，不把诊断权重用于 PPO。
