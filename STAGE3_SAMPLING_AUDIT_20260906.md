# Stage3 sampling 后续检查（2026-09-06）

## Material Passport

- Origin Skill: academic-research-suite / experiment-agent
- Origin Mode: run
- Origin Date: 2026-09-06
- Verification Status: protocol frozen before execution; results pending
- Version Label: sampling_audit_v1
- 数据范围：内部实验数据，不上传；不读取 Finalblind 结果。

## 目的和边界

解释为什么当前 C0 的全程 sampling 明显弱于 greedy，区分评估入口、历史输入、随机角色和采样强度的影响。
本次只做推理检查，不更新权重，不启动 RL，不修改上一轮未通过的训练准入结论。
未取得用户原先 sampling 实验的 checkpoint/目录，因此不能声称重现了那一次历史实验。

运行基于上一轮 `resume_20260905_1717/source` 的校验副本，仅增加独立诊断文件。
不修改现有 policy、environment、runner，也不因其他基线实验修改共享源码而中断。
C0 文件、案例文件和实际执行快照必须保持不变。

## 固定设计

- C0：Stage2 adaptive soft BC 的 `checkpoint_Epoch3.pt`，SHA256 `41cfc782c1ff908bb527c0219b75b73a1421d48a2dafa02d449015abbe5ab030`。
- 从 TrainDiag32 按固定种子 `2026090601`、分布和 profile 分层抽取 8 例：4 IID、3 stress、1 scale。不按效果挑例。
- 每组每例 32 个种子；沿用上一轮 `trajectory_seed(20260909, content_sha256, 0, replica)`，replica 0–31。
- 7 组 × 8 例 × 32 = 1792 条新采样轨迹，另有 8 条 greedy 和 8 条旧轨迹复现检查。
- 相同种子是配对标签；角色变化后随机数消耗顺序不同，不声称各角色使用相同随机数。

| 组别 | 随机角色 | 温度 | 候选限制 | 历史输入 |
|---|---|---:|---|---|
| full_t030 | 飞机、设备、牵引车 | 0.30 | 全部合法候选 | 环境权威历史 |
| plane_t030 | 仅飞机，其余 greedy | 0.30 | 全部合法候选 | 环境权威历史 |
| resource_t030 | 仅设备、牵引车，飞机 greedy | 0.30 | 全部合法候选 | 环境权威历史 |
| full_t010 | 全部 | 0.10 | 全部合法候选 | 环境权威历史 |
| full_t003 | 全部 | 0.03 | 全部合法候选 | 环境权威历史 |
| full_top4_t030 | 全部 | 0.30 | 每次决策最多 4 个合法候选 | 环境权威历史 |
| full_previous_t030 | 全部 | 0.30 | 全部合法候选 | 前一步发出的动作 |

Top-4 在现有自回归合法掩码内部取前 4 名，并重新归一化概率；不改变环境约束或请求占用顺序。
旧历史输入组只隔离保留的评估脚本中的历史输入差异，不同时切换环境、模型或终止规则。

## 一致性准入

四个 GPU 各检查 2 例，全过程逐状态比较 `act` 和 `get_actions`：输入、RNG 状态相同，动作及 RNG 消耗完全一致，hidden 最大绝对误差不超过 1e-5。
8 例 greedy 成本复现 C0，8 例 replica 0 的完整动作序列和成本复现上一轮档案；成本容差 1e-6。
全部通过后才自动启动 7 组对照。全角色原温度组的全部 256 条轨迹还会再次逐条核对旧档案。
所有阶段检查模型状态未改变。轨迹未完整完成、发生循环、输入/代码快照变化或一致性失败时停止，不将异常短轨迹当作优解。

数值容差修订记录：r1 使用 hidden 绝对容差 1e-6，在第 95 次调用遇到 1.1623e-6 而退出，原清单和日志保留。
独立 150 步同状态重复检查发现，同一个 `act` 接口的 hidden 波动也可达到 2.5779e-6，且动作/RNG 均一致。
r2 因此采用 FP32 hidden 容差 1e-5；动作、成本和完整性标准不变，不覆盖或伪装 r1 为成功。

## 资源、落盘与停止条件

- 物理 GPU0–3；CPU0–31,64–95（32 物理核 / 64 逻辑线程），与 GPU4–7 的基线实验隔离。
- 每个 GPU 8 个环境进程；每个推理进程 Torch/BLAS 单线程。
- 整个检查 cgroup 内存上限 40 GiB，软限制 32 GiB。启动前检查 GPU0–3 没有无关计算进程。
- 每 8 条轨迹保存不可覆盖的 JSON 元数据和压缩动作轨迹；每 30 秒记录心跳，每 50 步记录实际 rollout 进度。
- 无进展 10 分钟仅告警；环境 IPC 超时 300 秒，单轨迹最多 4000 步，一致性阶段最多 2 小时，采样阶段最多 24 小时，总时限 26 小时。
- 不自动重试、修改参数或停止无关进程。失败保存日志，后续恢复需单独审计已落盘组。

## 报告与解释

逐案例报告全部样本平均成本、标准差、单样本胜率和 best-of-1/4/8/16/32；同时报告保留 C0 为 incumbent 后的成本。
保存各角色的合法候选数、greedy 动作概率、有效分布熵、偏离 greedy 比例和资源 no-op 比例；只有多个合法候选的决策进入相应均值。
其中 base 概率指该组温度下、Top-k 限制之前的分布，不是所有组统一使用 τ=0.3。
8 例是机制诊断，不是泛化验证；不能据此宣布网络结构有/无问题，或宣布 RL 达到了提升目标。
不按这批结果自动选择新训练参数；完成后结合逐案例结果决定下一轮受控验证。
