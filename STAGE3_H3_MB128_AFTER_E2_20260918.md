## Material Passport

- Origin Skill: academic-research-suite / experiment-agent
- Mode: authorized checkpoint-boundary batch-size update
- Authorization: use 128/128 after the next complete checkpoint; do not interrupt epoch 2
- Verification: CPU tests and exact checkpoint-state comparison; native GPU restore and 128-wide capacity run after the boundary
- Research status: larger minibatch is a declared algorithm change; throughput and learning benefit are unverified

# Stage3 128/128 update after epoch 2

The running source is `stage3_h3_single_parent_20260918_r3_env384_gpu0`.
Its epoch 2 must finish both PPO passes and post-update replay, and publish the
complete checkpoint plus atomic commit ledger, before the watcher may stop the
old controller. A checkpoint filename without the complete ledger is insufficient.
The current incomplete epoch is never discarded. If the controller exits before
the boundary, or the bound commit is invalid, no automatic restart occurs.

The target is `stage3_h3_single_parent_20260918_r4_env384_mb128_gpu0`.
It keeps B_SHARED, H3/F4/soft, Train240, 384 trajectories/epoch, 384 concurrent
environments, 64 CPU environment processes, one model and one training flow.
Only optimizer minibatch and physical microbatch change from 64/64 to 128/128.
Two PPO passes, tau .03, all learning rates, TBPTT, clipping, KL gates, evaluation
rules and the original wall-clock budget are preserved. IGA stays frozen.

Local epochs 1 and 2 retain 12 updates each: 24 new updates plus the original
96 inherited updates, for 120 cumulative updates at the switch. Local epochs
3--8 have six updates each. Cumulative updates after epoch 3 are 126, and after
epoch 8 are 156, conditional on the existing validation/early-stop gates.
Training visits remain 3072 across all eight local epochs; the new optimizer
budget is 60 rather than 96 updates. No learning-rate compensation is applied.

Original checkpoints and results remain untouched. Copies rebind only manifest,
recipe and physical-microbatch metadata. Model, Adam moments and step counters,
ValueNorm, RNG and data cursor are compared exactly after serialization. Imported
epoch reports retain their actual historical microbatch and measurements.

The new service initially runs a CPU-only watcher. After the checkpoint is ready,
it prepares and verifies the new suite before signalling the old controller.
The old worker/controller is allowed to shut down; the new controller waits for
GPU0 to be free. Native restore canaries and short 128-wide capacity windows run
before formal continuation. These windows do not claim full-trajectory throughput
or numerical equivalence with 64/64. Missing epoch-2 Validation120 is evaluated
before proceeding to epoch 3. Existing complete evaluations are reused by hash.

GPU0 and CPU 0--31,64--95 remain the only training allocation. CPU subdivisions
and root-owned systemd memory-policy exemption are preserved. Global oomd is
unchanged. A failed request/probe is recorded; no silent retry or alternate batch
size is permitted. The watcher is independent of the Codex session.
